%table.table.properties
  %thead
    %tr
      %th.title= t('.title')
      %th= t('.category')
      %th= t('.value')
  %tbody
    = render @properties
    = paginate @properties, theme: 'adminka'

= paginate @properties, theme: 'adminka'
