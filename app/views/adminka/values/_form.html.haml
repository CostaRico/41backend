= form_for [:adminka, @value], html: { class: "form-horizontal" } do |f|
  .form-group
    %label.col-sm-2.control-label{ for: 'name' } Title
    .col-sm-5
      = f.text_field :value, class: 'form-control'
  .form-group
    .col-sm-5.col-md-offset-2
      = f.submit (@value.new_record? ? 'Create' : 'Save'), class: 'btn btn-default'
